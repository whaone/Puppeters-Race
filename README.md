# Puppeters-Race
Stripped out the complex browser-side setInterval injections. Puppeteer’s native page.on('response') interceptor works continuously on the backend as long as Strava's page updates its map data.
